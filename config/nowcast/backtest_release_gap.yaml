task:
  mode: "nowcast"
  estimate_unreleased_only: false

nowcast:
  run_name: "production_market_only_backtest_v1"
  models:
    - "rolling_mean"
    - "ewma"
    - "ridge"
  primary_model: "ridge"
  min_train_rows: 1000
  train_window_rows: 2520
  backtest_step_rows: 21
